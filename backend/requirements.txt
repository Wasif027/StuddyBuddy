# =============================================================================
# StudyBuddy — Backend dependencies
# =============================================================================

# --- Web framework -----------------------------------------------------------
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
sse-starlette>=2.1

# --- Validation & configuration --------------------------------------------
pydantic>=2.7
pydantic-settings>=2.2
jsonschema>=4.21

# --- Auth ------------------------------------------------------------------
pyjwt>=2.9
bcrypt>=4.2

# --- Data layer ------------------------------------------------------------
sqlalchemy>=2.0
psycopg[binary]>=3.1
pgvector>=0.3.6
redis>=5.0

# --- AI / RAG ------------------------------------------------------------
anthropic>=0.40
openai>=1.40
numpy>=1.26

# --- Study-material parsing -------------------------------------------------
pypdf>=4.2
pypdfium2>=4.30        # rasterise scanned/handwritten PDF answers for the vision model
python-docx>=1.1       # .docx notes
python-pptx>=1.0       # .pptx decks
pillow>=10.3           # image normalisation for vision ingest

# --- Export --------------------------------------------------------------
fpdf2>=2.7             # markdown -> PDF, pure Python (no system deps)

# --- Observability -------------------------------------------------------
opentelemetry-api>=1.25
opentelemetry-sdk>=1.25
opentelemetry-instrumentation-fastapi>=0.46b0
opentelemetry-instrumentation-sqlalchemy>=0.46b0
opentelemetry-instrumentation-redis>=0.46b0
opentelemetry-exporter-otlp-proto-http>=1.25
prometheus-fastapi-instrumentator>=7.0
structlog>=24.1

# --- Testing (kept here so the CI image can run the suite) --------------
pytest>=8.2
pytest-asyncio>=0.23
pytest-cov>=5.0
httpx>=0.27          # required by fastapi.testclient
ruff>=0.5
