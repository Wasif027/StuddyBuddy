{
  "name": "enterprise-knowledge-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Enterprise AI Knowledge & Decision Platform — hybrid RAG with citations, confidence and controlled actions.",
  "scripts": {
    "dev": "concurrently -k -n api,web -c yellow,cyan \"npm:dev:api\" \"npm:dev:web\"",
    "dev:api": "cd backend && python -m uvicorn app.main:app --reload --port 8000",
    "dev:web": "cd frontend && next dev",
    "setup": "npm --prefix frontend ci && pip install -r backend/requirements.txt",
    "test": "npm --prefix frontend run typecheck && cd backend && python -m pytest",
    "lint": "npm --prefix frontend run lint && cd backend && ruff check app tests"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
